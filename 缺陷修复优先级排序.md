# 视频下载器缺陷修复优先级排序

> 基于用户视角的全面代码分析，按严重程度和影响范围排序。

---

## P0 - 立即修复（可能导致崩溃/数据丢失）

| # | 问题 | 涉及文件 | 影响 |
|---|------|----------|------|
| 1 | ~~M3U8 并发下载无限制，可能导致内存溢出/App 崩溃~~ ✅ 已修复 (2026-06-19) | `M3U8DownloadHandler.swift` | 新增 `AsyncSemaphore` actor 限制片段并发下载数为 6，使用 `CheckedContinuation` 实现异步等待，`defer { Task { await semaphore.signal() } }` 确保异常时释放信号量。编译通过。 |
| 2 | ~~M3U8 合并片段全部加载到内存，大文件会 OOM~~ ✅ 已修复 (2026-06-19) | `M3U8DownloadHandler.swift` | 将 `Data(contentsOf:)` 替换为 `InputStream` + 固定 256KB 缓冲区流式复制，内存峰值恒定不随视频大小增长。编译通过。 |
| 3 | ~~`deleteDownloadTask` 逻辑错误，已完成文件永远不会被删除~~ ✅ 已修复 (2026-06-19) | `VideoDownloadEngine.swift` | 先保存 `completedURL` 和 `isCompleted` 状态，再执行 `cancel()`，避免状态被修改后条件判断失效。编译通过。 |
| 4 | ~~批量删除不清理已完成文件~~ ✅ 已修复 (2026-06-19) | `BatchDownloadManager.swift` | 改用 `deleteDownloadTask` 替代 `cancelDownload`，确保已完成文件被清理。编译通过。 |
| 5 | ~~`BatchDownloadManager` 双队列管理器导致并发控制失效~~ ✅ 已修复 (2026-06-19) | `BatchDownloadManager.swift` | 移除 BatchManager 独立的 `queueManager`，统一复用 `VideoDownloadEngine` 的队列。`startBatchDownload` 改为检查任务是否已在 Engine 队列中，避免重复添加。`getBatchProgress` 改为 async 从 Engine 获取最新状态。编译通过。 |
| 6 | ~~数据库已实现但从未被使用，App 杀死后所有进度丢失~~ ✅ 已修复 (2026-06-19) | `VideoDownloadEngine.swift`、`DownloadTaskProtocol.swift`、`MP4DownloadHandler.swift`、`M3U8DownloadHandler.swift`、`AppDelegate.swift` | 扩展 `DownloadTask` 协议添加 `format/totalSize/downloadedSize/createdAt/completedAt/resumeData` 字段；为 MP4/M3U8/Resumable 任务添加缺失属性与 `toDownloadItem()` 转换；Engine 初始化 `DownloadTaskDatabase`，任务创建/状态变更时 debounce 0.5s 写入，取消/删除时清理记录；App 启动调用 `restoreTasksFromDatabase()` 恢复未完成任务（MP4 支持 resumeData 恢复，M3U8/Thunder 暂跳过）。编译通过。 |

---

## P1 - 尽快修复（功能异常/可靠性差）

| # | 问题 | 涉及文件 | 影响 |
|---|------|----------|------|
| 7 | ~~后台下载未集成，App 后台时下载会暂停~~ ✅ 已修复 (2026-06-19) | `BackgroundDownloadSession.swift`、`MP4DownloadHandler.swift`、`VideoDownloadEngine.swift` | `MP4DownloadTask` 根据 `enableBackgroundDownload` 配置选择使用 `BackgroundDownloadSession`（后台 URLSession）或 `NetworkClient`（前台）。后台模式支持 resumeData 断点续传、自定义请求头。`BackgroundDownloadSession` 新增 URLRequest 支持、线程安全（串行队列）、handler 重新注册（App 重启恢复）、移除自动文件移动（由 MP4DownloadTask 统一处理）。`VideoDownloadEngine.restoreTasksFromDatabase` 在 App 重启后自动匹配仍在运行的后台任务并重新注册回调。编译通过。 |
| 8 | ~~`UIBackgroundModes` 配置错误，缺少 `download` 模式~~ ✅ 已修复 (2026-06-19) | `Info.plist` | 在 `UIBackgroundModes` 数组中添加 `download`，与原有 `fetch` 并列，授权 iOS 后台 URL 会话下载能力。编译通过。 |
| 9 | M3U8 断点续传状态不持久化，App 重启丢失进度 | `M3U8DownloadHandler.swift` | `completedSegments` 仅在内存中，重启后全部重新下载 |
| 10 | M3U8 进度/速度计算使用片段数而非字节数 | `M3U8DownloadHandler.swift` | 速度显示为 "5 B/s" 而非实际 MB/s，剩余时间完全错误 |
| 11 | ~~MP4 暂停时序竞态，可能导致下载失败而非暂停~~ ✅ 已修复 (2026-06-20) | `MP4DownloadHandler.swift` | 引入 `TaskTerminationReason` 状态机（`.pauseRequested`/`.cancelRequested`/`.none`），在 `pause()`/`cancel()` 中设置意图状态，`defer` 确保重置。前台/后台下载 Task 的 catch 块中检查 `terminationReason`，暂停导致的 `CancellationError`/`NSURLErrorCancelled` 不再发送 `.cancelled`/`.failed` 状态，避免与 `pause()` 发送的 `.paused` 冲突。编译通过。 |
| 12 | ~~文件下载（`downloadFileWithResumeCancellable`）无重试机制~~ ✅ 已修复 (2026-06-20) | `NetworkClient.swift` | 提取 `performSingleDownload` 私有方法，在 `downloadFileWithResumeCancellable` 外层添加 `for attempt in 0..<retryCount` 重试循环 + 指数退避，与 `downloadString`/`downloadData` 保持一致。编译通过。 |
| 13 | ~~`downloadFileWithResumeCancellable` 失败时未提取 resumeData~~ ✅ 已修复 (2026-06-20) | `NetworkClient.swift` | 为 `performSingleDownload` 添加 `resumeDataHandler`，在 `urlSession(_:task:didCompleteWithError:)` 触发时捕获 resumeData，失败时包装为 `NetworkError.resumeError(underlying:resumeData:)`。编译通过。 |
| 14 | ~~通知权限未请求，本地通知不会显示~~ ✅ 已修复 (2026-06-20) | `SceneDelegate.swift` | 在 `scene(_:willConnectTo:options:)` 中添加 `UNUserNotificationCenter.requestAuthorization(options:[.alert,.sound,.badge])`，使用 `Task` 异步包装不阻塞 UI。编译通过。 |

---

## P2 - 体验提升（功能不完善/体验差）

| # | 问题 | 涉及文件 | 影响 |
|---|------|----------|------|
| 15 | ~~视频格式检测仅靠 URL 字符串，不支持 Content-Type 检测~~ ✅ 已修复 (2026-06-20) | `VideoDownloadEngine.swift`、`VideoFormat.swift`、`NetworkClient.swift`、`ThunderDownloadHandler.swift`、`BatchDownloadManager.swift`、`Utilities/VideoFormatDetector.swift`（新建） | 新建 `VideoFormatDetector` 工具类，采用三级检测策略：URL 字符串快速匹配 → HEAD 请求 Content-Type 检测 → 兜底 `.mp4`。`NetworkClient` 新增 `fetchResponseHeaders` 方法（带重试+指数退避）。`VideoFormat` 枚举扩展支持 `.webm/.mkv/.flv/.mov`。消除 `ThunderDownloadHandler` 中的重复检测代码。`BatchDownloadManager.getFileExtension` 改用共享检测逻辑。编译通过。 |
| 16 | ~~仅支持 MP4/M3U8，不支持 WebM/MKV/FLV/MOV 等常见格式~~ ✅ 已修复 (2026-06-20) | `DownloadHandlerProtocol.swift`、`MP4DownloadHandler.swift`、`VideoDownloadEngine.swift`、`M3U8DownloadHandler.swift`、`ThunderDownloadHandler.swift` | `DownloadHandlerProtocol.createTask` 新增 `format` 参数；`MP4DownloadHandler` 使用 `format.fileExtension` 生成正确文件扩展名；`MP4DownloadTask.format` 改为可注入属性（默认 `.mp4` 向后兼容）；`VideoDownloadEngine` 将检测到的格式传递给 handler；数据库恢复时使用保存的实际格式。编译通过。 |
| 17 | ~~M3U8 仅支持 AES-128，不支持密钥轮换和 SAMPLE-AES~~ ✅ 已修复 (2026-06-20) | `M3U8Models.swift`、`M3U8Parser.swift`、`M3U8DownloadHandler.swift`、`DownloadError.swift`、`VideoDownloadEngine.swift` | `M3U8Encryption` 新增 `keyFormat` 字段；`M3U8DownloadHandler.createTask` 收集所有唯一密钥 URL 批量下载（支持密钥轮换），检测 FairPlay DRM 并抛出 `keyFormatNotSupported`；`M3U8DownloadTask` 改用 `encryptionKeyCache: [URL: Data]` 字典替代单一 `encryptionKey`；`downloadSegment` 使用每片段对应密钥解密；新增 `AESCTRDecryptor` 支持 SAMPLE-AES（AES-128-CTR，无填充）；`VideoDownloadEngine.restoreTasksFromDatabase` 同步更新密钥缓存恢复逻辑。编译通过。 |
| 18 | ~~M3U8 不支持 fMP4 容器（`#EXT-X-MAP`）和字节范围（`#EXT-X-BYTERANGE`）~~ ✅ 已修复 (2026-06-20) | `M3U8Models.swift`、`M3U8Parser.swift`、`M3U8DownloadHandler.swift`、`DownloadError.swift` | 新增 `M3U8ByteRange`、`M3U8MapInfo` 结构体；`M3U8Segment` 新增 `byteRange` 和 `map` 字段；`M3U8MediaPlaylist` 新增 `isFMP4` 和 `map` 字段；解析器新增 `parseMapInfo`、`parseByteRange` 方法，支持 `#EXT-X-MAP` 和 `#EXT-X-BYTERANGE` 标签；`downloadSegment` 支持字节范围下载（HTTP Range header）和 fMP4 扩展名（`.m4s`）；`resume` 中先下载初始化片段（`init_segment.mp4`）；`mergeSegments` fMP4 分支先写 init segment 再写媒体片段；`calibrateDownloadedBytes` 根据容器类型使用对应扩展名。编译通过。 |
| 19 | ~~M3U8 不区分 VOD 和 Live 流~~ ✅ 已修复 (2026-06-20) | `M3U8Models.swift`、`M3U8Parser.swift`、`M3U8DownloadHandler.swift`、`DownloadError.swift` | `M3U8MediaPlaylist` 新增 `isLive` 和 `mediaSequence` 字段；解析器检测 `#EXT-X-ENDLIST` 标签（有则为 VOD，无则为 Live）；`M3U8DownloadHandler.createTask` 检测直播流抛出 `liveStreamNotSupported` 错误；`VideoDownloadEngine.restoreTasksFromDatabase` 跳过直播流恢复。编译通过。 |
| 20 | ~~批量创建时单个 URL 失败中断整个批量任务~~ ✅ 已修复 (2026-06-20) | `BatchDownloadManager.swift`、`VideoDownloadEngine.swift`、`BatchDownloadViewController.swift`、`BatchDownloadCell.swift` | 引入 `BatchFailedItem` 记录失败项，`BatchDownloadResult` 封装创建结果，`createBatchDownload` 改为返回结果对象而非抛出异常。扩展 `BatchState` 新增 `.partiallyFailed`。新增 `retryFailedItems` 支持重试失败项。UI 展示失败数量和重试入口。编译通过。 |
| 21 | ~~网络切换（WiFi/蜂窝）处理完全缺失~~ ✅ 已修复 (2026-06-20) | `NetworkMonitor.swift`（新建）、`VideoDownloadEngine.swift`、`DownloadTaskProtocol.swift`、`MP4DownloadHandler.swift`、`M3U8DownloadHandler.swift`、`ResumableDownloadTask.swift`、`Constants.swift`、`DownloadConfiguration.swift`、`AppDelegate.swift` | 新建 `NetworkMonitor` 单例封装 `NWPathMonitor`，通过 Combine 发布网络状态变更。`VideoDownloadEngine` 订阅状态变更，网络断开时延迟 0.5s 自动暂停所有下载任务（标记 `pauseReason`），网络恢复时延迟 2s 自动恢复因网络原因暂停的任务（用户手动暂停的不受影响）。新增 `PauseReason` 枚举区分暂停原因。支持蜂窝下载策略配置（`isCellularAllowed`）。防抖机制避免频繁切换导致状态抖动。编译通过。 |
| 22 | ~~存储空间检查仅执行一次，下载过程中无持续监控~~ ✅ 已修复 (2026-06-20) | `FileStorageManager.swift`、`MP4DownloadHandler.swift`、`M3U8DownloadHandler.swift`、`DownloadError.swift`、`DownloadTaskProtocol.swift` | `FileStorageManager` 新增 `hasEnoughSpaceForContinue` 和 `requiredSpaceForTask`；`MP4DownloadTask` 在前后台下载进度回调中调用 `checkStorageSpaceDuringDownload`，空间不足时自动 `pause(reason: .insufficientStorage)`；`M3U8DownloadTask` 在 `downloadSegment` 前检查空间，不足时抛出 `DownloadError.insufficientStorage` 并清理临时文件；`PauseReason` 新增 `insufficientStorage`。编译通过。 |
| 23 | ~~缓存清理机制未实现~~ ✅ 已修复 (2026-06-20) | `FileStorageManager.swift`、`VideoDownloadEngine.swift`、`AppDelegate.swift` | `FileStorageManager` 新增 `getCacheSize()`、`getCacheFileAge()`、`cleanExpiredCache()`（按过期天数清理）、`enforceCacheSizeLimit()`（LRU 策略清理到 80% 阈值）、`performFullCacheCleanup()`（组合调用）；`VideoDownloadEngine` 在 `observeTaskForDatabase` 任务结束、删除任务、清空下载时调用 `triggerCacheCleanup()`；`AppDelegate` 启动时执行缓存清理。编译通过。 |
| 24 | ~~`DownloadQueueManager` 状态回调中 `@MainActor` 绕过 actor 隔离~~ ✅ 已修复 (2026-06-20) | `DownloadQueueManager.swift` | 将 `subscribeToTaskState` 中的 `Task { @MainActor in ... }` 改为 `Task { ... }`，让 Swift 并发运行时自动 hop 回 actor executor 执行 `handleTaskStateChange`，确保对 `runningTaskIds`/`tasks`/`pendingQueue` 的读写在 actor 隔离域内串行化。同步更新 `MockDownloadTask` 补齐 `pauseReason` 和 `pause(reason:)` 协议要求。编译通过。 |
| 25 | ~~URL 输入框缺失，单任务页硬编码示例 URL~~ ✅ 已修复 (2026-06-20) | `ViewController.swift` | 添加 `urlTextField`（带 URL 键盘类型、清除按钮、Return 收起键盘），`startDownload()` 从输入框读取 URL 并校验有效性，支持键盘收起手势。编译通过。 |
| 26 | ~~批量下载页缺少暂停/恢复/重试操作~~ ✅ 已修复 (2026-06-20) | `BatchDownloadViewController.swift`、`BatchDownloadCell.swift` | 新增 `BatchDownloadCellDelegate` 协议，Cell 添加暂停/恢复/重试/取消按钮，根据状态动态显示。`BatchDownloadViewController` 实现委托，调用 `pauseBatchDownload`/`startBatchDownload`/`retryFailedItems`/`cancelBatchDownload`。编译通过。 |
| 27 | ~~批量下载页无实时进度刷新~~ ✅ 已修复 (2026-06-20) | `BatchDownloadViewController.swift` | 移除 2 秒 `Timer` 轮询，改为订阅 `DownloadNotification.progressDidUpdate` 和 `stateDidChange` 通知，通过 `refreshCellIfContains(taskId:)` 精准刷新包含该任务的 Cell。编译通过。 |
| 28 | ~~Cell 信息展示不足（无速度/剩余时间/文件大小）~~ ✅ 已修复 (2026-06-20) | `BatchDownloadCell.swift` | 新增 `infoLabel`，`updateProgress` 中聚合计算所有任务的 `downloadedSize` 和 `totalSize`，使用 `ByteCountFormatter` 显示 "已下载 / 总大小"；进度百分比格式改为 "3/5 (60%)"；Cell 高度从 100 调整为 140。编译通过。 |
| 29 | ~~状态文字显示英文 rawValue~~ ✅ 已修复 (2026-06-20) | `BatchDownloadManager.swift`、`DownloadState.swift`、`BatchDownloadCell.swift`、`ViewController.swift` | `BatchState` 和 `DownloadState` 枚举新增 `displayText` 属性返回中文状态；Cell 和单任务页日志统一使用 `displayText`。编译通过。 |
| 30 | ~~没有设置页面，所有参数硬编码~~ ✅ 已修复 (2026-06-20) | `SettingsViewController.swift`（新建）、`DownloadConfiguration.swift` | 新建 `SettingsViewController`，使用 `UserDefaults` 持久化 5 项配置（最大并发数、超时时间、重试次数、蜂窝网络、后台下载）。`DownloadConfiguration.default` 改为计算属性，从 `UserDefaults` 读取。编译通过。 |
| 31 | ~~两个 Tab（"下载管理"和"批量下载"）完全重复~~ ✅ 已修复 (2026-06-20) | `MainTabBarController.swift` | 移除重复的 "下载管理" Tab，保留 "单任务下载"、"批量下载"，新增 "设置" Tab（`SettingsViewController`）。编译通过。 |
| 32 | ~~ATS 全局禁用（`NSAllowsArbitraryLoads = true`）~~ ✅ 已修复 (2026-06-20) | `Info.plist` | `NSAllowsArbitraryLoads` 改为 `false`，添加 `NSAllowsLocalNetworking` 为 `true`。编译通过。 |

---

## P3 - 完善产品（锦上添花）

| # | 问题 | 涉及文件 | 影响 |
|---|------|----------|------|
| 33 | ~~`ResumableDownloadTask` 是死代码~~ ✅ 已修复 (2026-06-20) | `ResumableDownloadTask.swift`（已删除） | 全项目无引用且未参与编译，直接删除文件。验证：搜索 `ResumableDownloadTask` 无残留引用。 |
| 34 | ~~大量 `print` 调试语句~~ ✅ 已修复 (2026-06-20) | `VideoDownloadEngine.swift`、`BatchDownloadManager.swift`、`BatchDownloadViewController.swift` | 将 16 处 `print` 替换为 `Logger.info()`/`Logger.error()`，`ViewController.swift` 注释中的示例代码保留。验证：搜索 `print(` 仅剩注释中的 5 处。 |
| 35 | 并发数硬编码，无优先级机制 | `DownloadQueueManager.swift`、`DownloadTaskProtocol.swift` | 用户无法调整并发数或提前下载重要任务 |
| 36 | ~~迅雷链接仅支持经典 `thunder://` 格式~~ ✅ 已修复 (2026-06-20) | `ThunderParser.swift`、`ThunderDownloadHandler.swift`、`VideoFormatDetector.swift`、`VideoFormat.swift`、`DownloadError.swift`、`VideoDownloadEngine.swift` | `ThunderParser` 引入 `ParseResult` 结构体，支持 `thunder://` 和 `thunderp2p://` 两种前缀解析（Base64 解码后判断磁力链接/AA+ZZ 包装/直接 URL）；`VideoFormatDetector` 新增 `thunderp2p://` 和 `magnet:` 前缀识别；`VideoFormat` 新增 `.thunderP2P`/`.magnet` 枚举值；`ThunderDownloadHandler` 按 format 分支处理（`.magnet` 直接抛错、`.thunderP2P` 解析后判断、`.thunder` 保持原逻辑）；`DownloadError` 新增 `p2pDownloadNotSupported`/`magnetLinkNotSupported`；`VideoDownloadEngine.createHandler` 扩展分发。编译通过。 |
| 37 | ~~`DownloadTask` 协议缺少 `retry()` 方法~~ ✅ 已修复 (2026-06-20) | `DownloadTaskProtocol.swift`、`MP4DownloadHandler.swift`、`M3U8DownloadHandler.swift`、`VideoDownloadEngine.swift`、`ViewController.swift` | 协议新增 `retry()` 方法；`MP4DownloadTask` 实现：重置状态为 `.pending`，保留 resumeData，调用 `resume()`；`M3U8DownloadTask` 实现：重置状态为 `.pending`，保留已下载片段状态，调用 `resume()`；`VideoDownloadEngine` 新增 `retryDownload(task:)` 公共入口；`ViewController` 添加 Retry 按钮，仅在任务失败时显示。编译通过。 |
| 38 | ~~缺少"已完成文件/本地文件管理"页面~~ ✅ 已修复 (2026-06-20) | `CompletedFilesViewController.swift`（新建）、`CompletedFileCell.swift`（新建）、`CompletedFileDetailViewController.swift`（新建）、`CompletedFileItem.swift`（新建）、`EmptyStateView.swift`（新建）、`MainTabBarController.swift`、`FileStorageManager.swift`、`VideoDownloadEngine.swift` | 新建"已完成文件"Tab 页面，支持浏览/搜索/排序已完成文件；`CompletedFilesViewController` 使用 UITableView + UISearchController 展示文件列表，支持 3 种排序（完成时间/文件名/文件大小）；`CompletedFileCell` 自定义 Cell 展示文件图标/名称/大小/格式/时间；滑动删除 + 长按上下文菜单（预览/分享/详情/删除）；`UIActivityViewController` 分享文件（iPad 适配）；`QLPreviewController` 预览视频；`CompletedFileDetailViewController` 文件详情页展示元数据和操作入口；`EmptyStateView` 通用空状态组件；`FileStorageManager` 新增 `enumerateCompletedFiles()` 枚举方法；`VideoDownloadEngine.database` 改为 `private(set)` 供外部只读访问；订阅 `downloadDidComplete` 通知自动刷新列表。编译通过。 |
| 39 | ~~缺少视频预览/播放功能~~ ✅ 已修复 (2026-06-20) | `CompletedFilesViewController.swift`、`CompletedFileDetailViewController.swift`、`ViewController.swift`、`VideoPlayerViewController.swift`（新建） | 新建 `VideoPlayerViewController` 封装 `AVPlayerViewController`，支持全屏播放、播放控制、错误处理和播放完成自动退出。已完成文件列表页点击行优先播放视频，长按菜单和详情页均添加"播放"入口。单任务下载页下载完成后显示 Play 按钮。编译通过。 |
| 40 | ~~暗色模式未适配~~ ✅ 已修复 (2026-06-20) | `UIColor+Hex.swift`、`BatchDownloadViewController.swift`、`BatchDownloadCell.swift`、`CompletedFileCell.swift`、`CompletedFilesViewController.swift` | `UIColor+Hex` 新增 `dynamic(light:dark:)` 和 `dynamic(hex:darkHex:)` 工厂方法；`BatchDownloadVC` 5 处硬编码替换为语义颜色（`.systemGroupedBackground`、`.secondarySystemGroupedBackground`、`.label`、`.systemRed`、`.systemBlue`）；`BatchDownloadCell` 7 处替换（`.systemGreen/.systemRed/.systemOrange/.systemBlue` + 动态分隔线 `e0e0e0/3a3a3a` + `.tertiaryLabel`）；`CompletedFileCell` 4 处替换（`.systemBlue`、`.secondaryLabel`、动态标签背景 `f0f0f0/2c2c2e`、动态分隔线）；`CompletedFilesVC` 1 处替换（`.systemBlue`）。按钮文字 `.white` 在彩色背景上保留。编译通过。 |
| 41 | ~~中英文混用，无国际化方案~~ ✅ 已修复 (2026-06-20) | `ViewController.swift`、`VideoPlayerViewController.swift`、`CompletedFileCell.swift`、`Protocols/DownloadError.swift`、`Network/NetworkError.swift`、`Storage/StorageError.swift`、`Utilities/Strings.swift`（新建） | 新建 `Strings.swift` 集中管理中文 UI 字符串常量；`ViewController` 5 个按钮标题和 URL 输入框占位符改为中文；`VideoPlayerViewController` 播放失败错误消息改为中文格式；`CompletedFileCell` 无扩展名回退标签 "FILE" -> "文件"；`DownloadError`/`NetworkError`/`StorageError` 的 `errorDescription` 全部中文化（rawValue 保持英文不变）。全局搜索确认无用户可见英文 UI 字符串残留。编译通过。
| 42 | ~~无空状态页面/加载状态/错误引导~~ ✅ 已修复 (2026-06-20) | `BatchDownloadViewController.swift` | 新增 `EmptyStateView`（复用已有组件）和 `UIActivityIndicatorView`；`setupUI()` 添加视图及约束；新增 `updateEmptyState(isError:)` 方法，无任务时显示引导（icon: `tray`，标题: "暂无批量下载任务"，描述: "点击右下角 + 按钮添加批量下载任务"），加载失败时显示错误引导（icon: `exclamationmark.triangle`）；`loadBatchTasks()` 改造为加载开始时显示 loading indicator，完成后调用 `updateEmptyState()`，添加 `do/catch` 错误处理。编译通过。 |
| 43 | 无下拉刷新 | `BatchDownloadViewController.swift` | 用户无法手动刷新列表 |
| 44 | ~~`UIColor+Hex` 不支持 3 位缩写和 alpha 通道~~ ✅ 已修复 (2026-06-20) | `UIColor+Hex.swift` | `init(hex:)` 改为根据字符串长度(3/4/6/8)自动分派解析：3 位展开为 6 位 RGB，4 位展开为 8 位 RGBA，8 位解析 RR-GG-BB-AA（CSS 标准）。新增 `expandShorthand` 静态辅助方法，使用 `fallthrough` 复用解析逻辑。无效长度触发 `assertionFailure` 回退透明黑色。所有现有 6 位调用向后兼容。编译通过。 |
| 45 | ~~`SpeedCalculator` 两个方法逻辑重复~~ ✅ 已修复 (2026-06-20) | `SpeedCalculator.swift` | `calculateAverageSpeed()` 与 `calculateSpeed()` 逻辑完全相同，改为委托调用 `calculateSpeed()`，消除代码重复。方法签名不变，外部调用无影响。编译通过。 |
| 46 | ~~`SpeedCalculator` 无线程安全保护~~ ✅ 已修复 (2026-06-20) | `SpeedCalculator.swift` | `class` 改为 `final class`，添加 `NSLock` 私有锁，在 `addSample`/`calculateSpeed`/`calculateAverageSpeed`/`reset` 中使用 `lock.lock()` + `defer { lock.unlock() }` 保护 `samples` 数组的并发读写。新增并发安全单元测试验证多线程场景不崩溃。编译通过。 |
| 47 | ~~`Logger` 命名与 `os.Logger` 冲突，缺少 warning 级别~~ ✅ 已修复 (2026-06-20) | `Logger.swift` → `AppLogger.swift`（新建） | `struct Logger` 重命名为 `struct AppLogger`，消除与 `os.Logger` 命名冲突。`warning` 级别已存在无需新增。全项目 18 个文件 129 处 `Logger.` 调用批量替换为 `AppLogger.`。编译通过（`Strings.swift` 预存语法错误不影响本次修复）。验证：搜索 `struct Logger` 无残留，搜索 `\bLogger\.` 无残留调用。 |
| 48 | ~~`FileStorageManager.createDirectoryIfNeeded` 失败被静默吞掉~~ ✅ 已修复 (2026-06-20) | `FileStorageManager.swift`、`MP4DownloadHandler.swift`、`M3U8DownloadHandler.swift`、`VideoDownloadEngine.swift` | `createDirectoryIfNeeded` 改为 `throws`，失败时抛出 `StorageError.directoryCreationFailed`；5 个目录方法（`downloadsDirectory`/`inProgressDirectory`/`completedDirectory`/`cacheDirectory`/`createTaskDirectory`）添加 `throws`；`moveFile` 中添加 `try`；缓存管理方法（`getCacheSize`/`cleanExpiredCache`/`enforceCacheSizeLimit`/`enumerateCompletedFiles`）用 `try?` 安全降级；`M3U8DownloadTask.stateFileURL` 从计算属性改为 `throws` 方法；所有调用方按场景分别处理（`try` 或 `try?` + guard）。编译通过。 |
| 49 | ~~数据库未开启 WAL 模式，无事务保护~~ ✅ 已修复 (2026-06-20) | `DownloadTaskDatabase.swift` | `init()` 中 `sqlite3_open` 后添加 `PRAGMA journal_mode=WAL` 和 `PRAGMA busy_timeout=5000`；新增 `beginTransaction`/`commit`/`rollback` 事务辅助方法；`createTables()` 和 `migrateIfNeeded()` 包裹事务保护，失败时自动 ROLLBACK。编译通过。 |
| 50 | ~~数据库迁移缺乏幂等性保护~~ ✅ 已修复 (2026-06-20) | `DownloadTaskDatabase.swift` | 新增 `columnExists(_:in:)` 方法通过 `PRAGMA table_info` 检查列是否存在；`migrateV1ToV2()` 改为逐列检查后再 ALTER，已存在的列跳过；`migrateV2ToV3()` 同样添加幂等检查；配合问题 49 的事务保护，确保迁移和版本更新原子性。编译通过。 |

---

## 建议修复路线

### 第一阶段：稳定性（P0）
- M3U8 并发限制（信号量控制 5-10 个）+ 流式合并（`FileHandle`）
- 修复 `deleteDownloadTask`：先保存 `completedURL` 再 cancel
- 统一队列管理器：`BatchDownloadManager` 复用 `VideoDownloadEngine` 的 `queueManager`
- 集成 `DownloadTaskDatabase`：任务创建/状态变更时同步写入，启动时恢复

### 第二阶段：可靠性（P1）
- 集成 `BackgroundDownloadSession`，修复 `UIBackgroundModes` 配置
- 持久化 M3U8 下载状态（`completedSegments` 写入磁盘）
- 修复速度计算：使用实际下载字节数
- 文件下载增加重试逻辑（指数退避）
- 请求通知权限

### 第三阶段：体验（P2）
- 增强 M3U8 解析（密钥轮换、fMP4、VOD/Live 区分）
- 格式检测增加 HEAD 请求 Content-Type
- 添加 URL 输入框、设置页面、网络状态监听
- 修复 Tab 重复、添加暂停/恢复/重试 UI
- 批量创建容错（部分失败继续处理其余项）
- ATS 配置白名单域名

### 第四阶段：完善（P3）
- 文件管理页、视频预览、暗色模式、国际化
- 清理死代码和调试语句
- 优先级队列、更多格式支持
- 数据库 WAL 模式和事务保护
